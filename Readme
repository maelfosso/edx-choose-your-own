# Prerequisites

If you are using Debian or Ubuntu, kindly installed these packages before running the script.
```
sudo apt install libcurl4-openssl-dev libfreetype6-dev libjpeg-dev libpng-dev libfontconfig1-dev libfribidi-dev libharfbuzz-dev libxml2-dev libtiff-dev libicu-dev libssl-dev zlib1g-dev make pandoc
```

# MLflow

Kindly install mlflow and run it

To install it run `pip install mlflow`

To run it, run `mlflow server --host 127.0.0.1 --port 8080`

Then, run the `code.R` file.